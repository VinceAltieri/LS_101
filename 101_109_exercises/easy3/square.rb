# square.rb

def multiply(num1, num2)
  num1 * num2
end

def squared(num)
  multiply(num, num)
end

# further exploration
# a second argument can be added that represents the power
# we want to raise the number to. That's how many times we
# will enter the number into the multiply method.