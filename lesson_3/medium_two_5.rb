def color_valid(color)
  if color == "blue" || color == "green"
    true
  else
    false
  end
end

# We can redefine the method this way:
# def color_valud(color)
#   color == "blue" || color == "green"
# end

