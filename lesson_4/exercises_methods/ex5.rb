# ex5.rb

hash = { a: 'ant', b: 'bear' }
hash.shift

# Again we can look at the docs. The docs tell us that shift removes the
# key value pair and returns it as a two - item array.
# So here it will return [a:, 'ant']